import React from 'react'

import './styles.css'

const Building = ({ children }) => {
    return (
        <div className="Building">
            {children}
        </div>
    )
}

export default Building